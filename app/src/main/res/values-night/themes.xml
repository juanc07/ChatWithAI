<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ChatWithAI" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Background color -->
        <item name="android:windowBackground">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/purple_700</item>
        <!-- ActionBar color -->
        <item name="android:textColorPrimary">@color/white</item>
        <!-- Button colors -->
        <!--<item name="colorControlNormal">@color/purple_200</item>-->
        <item name="colorControlNormal">@color/white</item> <!-- Change the color here -->
    </style>

    <style name="Theme.ChatWithAI.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- ActionBar Style -->
    <style name="AppTheme.ActionBarStyle" parent="Widget.MaterialComponents.ActionBar.Primary">
        <item name="titleTextColor">@color/white</item>
        <item name="android:background">@color/purple_700</item>
        <item name="colorControlNormal">@color/back_button_color</item>
    </style>
</resources>
